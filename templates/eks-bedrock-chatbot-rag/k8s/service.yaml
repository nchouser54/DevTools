apiVersion: v1
kind: Service
metadata:
  name: bedrock-chatbot-rag
  namespace: chatbot
  labels:
    app: bedrock-chatbot-rag
spec:
  selector:
    app: bedrock-chatbot-rag
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
