apiVersion: v1
kind: Service
metadata:
  name: bedrock-chatbot-connectors
  namespace: chatbot
  labels:
    app: bedrock-chatbot-connectors
spec:
  selector:
    app: bedrock-chatbot-connectors
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
