# Connector secrets — never commit live values.
# Populate with kubectl or External Secrets Operator (see eks-bedrock-chatbot-secure-enterprise).
apiVersion: v1
kind: Secret
metadata:
  name: bedrock-chatbot-connectors
  namespace: chatbot
type: Opaque
stringData:
  GITHUB_PERSONAL_ACCESS_TOKEN: "replace-me"
  JIRA_API_TOKEN: "replace-me"
  JIRA_USER_EMAIL: "you@example.com"
  CONFLUENCE_API_TOKEN: "replace-me"
  CONFLUENCE_USER_EMAIL: "you@example.com"
