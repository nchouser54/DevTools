apiVersion: v1
kind: Service
metadata:
  name: bedrock-chatbot
  namespace: chatbot
  labels:
    app: bedrock-chatbot
spec:
  selector:
    app: bedrock-chatbot
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
