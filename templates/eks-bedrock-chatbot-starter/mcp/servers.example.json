{
  "servers": {
    "filesystem": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "${MCP_ALLOWED_ROOT}"
      ],
      "enabled": true,
      "notes": "Restrict filesystem access to approved workspace roots only."
    },
    "github": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-github"
      ],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${MCP_GITHUB_TOKEN}"
      },
      "enabled": false,
      "notes": "Keep disabled until a reviewed token and policy are in place. See eks-bedrock-chatbot-connectors for a fully-wired version."
    }
  }
}
