apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "eks-bedrock-chatbot.fullname" . }}
  namespace: {{ .Values.app.namespace }}
  labels:
    {{- include "eks-bedrock-chatbot.labels" . | nindent 4 }}
  annotations:
    eks.amazonaws.com/role-arn: {{ .Values.aws.irsaRoleArn | quote }}
